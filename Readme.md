# HavenSync API

ASP.NET Core backend for HavenSync.

## Prerequisites
- .NET SDK (8.0 or compatible)
- SQL Server (LocalDB / SQL Express / full SQL Server)

## Getting Started

### 1. Clone the repository
```bash
git clone <repo-url>
cd HavenSync_api
